# 브랜드 리드(영업) 파이프라인 — 사용 가이드

AI Fashion Lookbook Studio를 한국(무신사/29CM/브랜디 입점 브랜드) · 미국 · 일본 패션 브랜드에
영업하기 위한 **1단계: 수집 → 분류 → 분석 → 아웃리치 초안 생성** 도구입니다.

관리자 페이지: `/admin/leads` (기존 `/admin02`와 동일한 `X-Admin-Password` 로그인 사용)

## 왜 "1단계"만 만들었나

전체 국가 x 전 과정(수집·분류·분석·마케팅·영업 발송)을 한 번에 완전 자동화하면
다음과 같은 실질적 위험이 있어, 우선 가장 안전하고 재사용 가능한 기반부터 구축했습니다.

- 무신사/29CM/브랜디, 미국/일본 주요 플랫폼 대부분은 **이용약관상 자동 스크래핑을 제한**합니다.
- 담당자 연락처를 무단 수집해 대량 발송하면 **정보통신망법(한국), CAN-SPAM(미국), 特定電子メール法(일본)** 위반 소지가 있습니다.
- 그래서 이 도구는 **① 브랜드명·카테고리·URL 등 공개 정보만 수집**하고, **② 발송은 자동화하지 않고 초안만 생성**합니다.

## 데이터베이스 마이그레이션 적용

```bash
# 로컬(D1 local)
npx wrangler d1 execute LOOKBOOK_DB --local --file=migrations/0009_brand_leads.sql

# 운영(D1 production) — 배포 파이프라인에 맞게 실행
npx wrangler d1 execute LOOKBOOK_DB --remote --file=migrations/0009_brand_leads.sql
```

## (선택) AI 분석/초안 고도화

`ANTHROPIC_API_KEY` 시크릿을 설정하면 브랜드 분석과 이메일 초안 작성에 Claude API를 사용합니다.
설정하지 않으면 키워드 기반 휴리스틱 분류 + 템플릿 초안으로 자동 동작합니다(둘 다 즉시 사용 가능).

```bash
npx wrangler secret put ANTHROPIC_API_KEY
```

## 워크플로

1. **플랫폼 설정 탭**: 국가별 플랫폼(무신사/29CM/브랜디, Faire 등)을 등록. 스크래핑을 쓰려면
   `directory_url`과 `list_selector`(브랜드 링크 `<a>` 요소의 CSS 셀렉터)를 해당 페이지 HTML을
   직접 확인해 입력하고, 이용약관/robots.txt를 검토한 뒤에만 "스크래핑 허용"을 체크하세요.
2. **브랜드 수집 탭**:
   - **CSV 가져오기(권장)**: `name,category,brand_url,contact_email` 헤더의 CSV를 붙여넣거나 업로드.
   - **스크래핑 실행**: 플랫폼에서 스크래핑을 허용한 경우에만 동작하며, 서버가 매 실행마다
     `robots.txt`를 자동 확인해 금지된 경로면 중단합니다. 담당자 연락처는 수집하지 않습니다.
3. **브랜드 목록/분석 탭**: 브랜드를 선택 후 "선택 분석 실행" → 스타일 태그·가격대·룩북 수요 점수·
   영업 우선순위 점수가 산출됩니다.
4. **브랜드 목록/분석 탭**에서 이어서 "선택 초안 생성" → 국가에 맞는 언어(한국어/영어/일본어)로
   이메일 제목/본문 초안이 생성됩니다(상태: `draft`).
5. **아웃리치 초안 탭**: 초안을 검토·수정하고, 실제 발신자 정보와 수신거부 안내를 채운 뒤
   본인 메일함(Gmail 등)에서 **직접 발송**하고 상태를 `발송완료`로 표시하세요.
6. 언제든 **CSV 내보내기**로 브랜드+분석+초안 전체를 다운로드해 별도 CRM/메일 도구로 옮길 수 있습니다.

## 다음 단계로 고려할 수 있는 것

- 실제 발송을 자동화하려면 SendGrid/Resend 같은 발신 인프라 연동, 발신 도메인 인증(SPF/DKIM),
  수신거부 링크 처리, 발송 빈도 제한을 먼저 갖춰야 합니다.
- 플랫폼별 스크래퍼는 각 사이트 DOM 구조를 검증한 뒤 셀렉터를 채워야 실제로 동작합니다
  (현재는 코드/구조만 준비되어 있고, 국가별 실제 플랫폼 셀렉터는 시드되어 있지 않습니다).
- 상담/견적 요청, 응답률 등을 추적하는 CRM 대시보드(2단계)로 확장할 수 있습니다.
